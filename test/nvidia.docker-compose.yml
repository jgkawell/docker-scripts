version: '3'
services:
  main:
    image: jgkawell/test:nvidia
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix:rw"
      - "${XAUTH}:${XAUTH}"
    build:
      context: .
      dockerfile: nvidia.Dockerfile
    container_name: test-nvidia
    environment:
      - "DISPLAY=${DISPLAY}"
      - "QT_X11_NO_MITSHM=1"
      - "CONTAINER_NAME=test-nvidia"
      - "XAUTHORITY=${XAUTH}"
    stdin_open: true
    tty: true
