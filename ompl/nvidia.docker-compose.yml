version: '3'
services:
  main:
    image: jgkawell/ompl-dev:base-nvidia
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix:rw"
      - "${XAUTH}:${XAUTH}"
    build:
      context: .
      dockerfile: nvidia.Dockerfile
    container_name: ompl-dev-base-nvidia
    environment:
      - "DISPLAY=${DISPLAY}"
      - "QT_X11_NO_MITSHM=1"
      - "CONTAINER_NAME=ompl-dev-base-nvidia"
      - "XAUTHORITY=${XAUTH}"
    stdin_open: true
    tty: true
