version: '3'
services:
  main:
    image: jgkawell/ompl-dev:sawyer-nvidia
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix:rw"
      - "${XAUTH}:${XAUTH}"
    build:
      context: ../
      dockerfile: ./sawyer/nvidia.Dockerfile
    container_name: ompl-dev-sawyer-nvidia
    environment:
      - "DISPLAY=${DISPLAY}"
      - "QT_X11_NO_MITSHM=1"
      - "CONTAINER_NAME=ompl-dev-sawyer-nvidia"
      - "XAUTHORITY=${XAUTH}"
    stdin_open: true
    tty: true
