version: '3'
services:
  main:
    image: jgkawell/ompl-dev:lfd-nvidia
    volumes:
      - "/tmp/.X11-unix:/tmp/.X11-unix:rw"
      - "${XAUTH}:${XAUTH}"
    build:
      context: ../
    container_name: ompl-dev-lfd
    environment:
      - "DISPLAY=${DISPLAY}"
      - "QT_X11_NO_MITSHM=1"
      - "CONTAINER_NAME=ompl-dev-lfd"
      - "XAUTHORITY=${XAUTH}"
    stdin_open: true
    tty: true
